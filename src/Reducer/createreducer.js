import React from "react";

const initialData = {
  studentname: "Hardik",
  studentId: 104,
};

const createreducer = (state = initialData, action) => {
  return state;
};

export default createreducer;
